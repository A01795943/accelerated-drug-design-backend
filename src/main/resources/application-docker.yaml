# Perfil docker: core y BD desde variables de entorno
# Requiere: CORE_URL, DB_URL, DB_USERNAME, DB_PASSWORD
app:
  core:
    base-url: ${CORE_URL}

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
